# stream-eval configuration. Copy to .env locally; .env is gitignored.

# Model identifier for `claude -p --model`. Pin the exact gateway-accepted
# string (e.g. claude-sonnet-4-6) rather than the `sonnet` alias, which
# resolves to the older Sonnet on this gateway.
STREAM_EVAL_MODEL=sonnet

# Recent-fallback window for session detection in the dashboard.
# Stale .output files older than this don't surface as "this session."
DASHBOARD_MAX_AGE_HOURS=4
